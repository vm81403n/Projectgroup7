 pipeline {
  agent any 
  tools {
    maven 'Maven'
  }
  stages {
 
   stage('Scan') {
      steps {
        sh 'grype dir:. --scope AllLayers'
      }
    }

   stage ('Build') {
      steps {
      sh 'mvn clean package'
      }
    }

    
  }
}
