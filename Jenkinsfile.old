 pipeline {
  agent any 
  tools {
    maven 'Maven'
  }
  stages {
    stage ('Build') {
      steps {
      sh 'set +e && mvn clean package -Dcheckstyle.skip && set -e'
      }
    }

   stage('Scan') {
      steps {
        sh 'grype dir:. --scope AllLayers'
      }
    }
  

    
  }
}
